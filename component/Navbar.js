import Link from 'next/link'
import styles from '../css/navbar.module.css'

const Navbar = (props) => (
    <nav className="navbar navbar-expand-lg navbar-dark  bg-dark">
        <a className="navbar-brand" href="#">Solicitudes de Credito</a>     
    </nav>
);

export default Navbar;  